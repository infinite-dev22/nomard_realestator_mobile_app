import 'package:flutter/material.dart';

import '../widget/search_layout.dart';

class Search extends StatelessWidget {
  const Search({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: SearchLayout());
  }
}
